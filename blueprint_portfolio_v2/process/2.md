# Ψ-IMPLEMENTAÇÃO: COMPONENTES REPETITIVOS RESTANTES

Vou implementar os componentes Ψ-repetitivos seguindo o mesmo padrão da implementação anterior.

---

## **1. COMPONENTE AURA-CARD**

```tsx
// src/app/components/ui/AuraCard/AuraCard.tsx
'use client';

import { ReactNode } from 'react';
import Link from 'next/link';
import styles from './AuraCard.module.css';

export interface AuraCardProps {
  variant?: 'project' | 'tool' | 'principle' | 'compact';
  density?: 'light' | 'ideal' | 'dense';
  category?: string;
  time?: string;
  title: string;
  excerpt: string;
  href?: string;
  tags?: Array<{
    label: string;
    type: 'complexity' | 'impact' | 'status' | 'custom';
    value?: string;
  }>;
  progress?: number;
  ctaText?: string;
  children?: ReactNode;
  onCtaClick?: () => void;
}

export default function AuraCard({
  variant = 'project',
  density = 'ideal',
  category,
  time,
  title,
  excerpt,
  href,
  tags = [],
  progress,
  ctaText = 'Ver detalhes',
  children,
  onCtaClick,
}: AuraCardProps) {
  const CardContainer = href ? Link : 'div';
  
  const containerProps = href ? {
    href,
    className: styles.cardLink
  } : {
    className: styles.cardContainer
  };

  return (
    <article 
      className={`${styles.card} ${styles[variant]} ${styles[`density-${density}`]}`}
      data-density={density}
      data-precision="high"
    >
      <CardContainer {...containerProps}>
        {/* Header com categoria e tempo */}
        {(category || time) && (
          <div className={styles.header}>
            {category && (
              <span 
                className={styles.category}
                data-category={category.toLowerCase().replace(/\s+/g, '-')}
              >
                {category}
              </span>
            )}
            {time && (
              <time className={styles.time} dateTime={time}>
                {time}
              </time>
            )}
          </div>
        )}

        {/* Conteúdo principal */}
        <div className={styles.body}>
          <h3 className={styles.title}>
            {title}
          </h3>
          
          <p className={styles.excerpt}>
            {excerpt}
          </p>

          {/* Tags de complexidade/impacto */}
          {tags.length > 0 && (
            <div className={styles.tags}>
              {tags.map((tag, index) => (
                <span 
                  key={index}
                  className={styles.tag}
                  data-type={tag.type}
                  data-value={tag.value}
                >
                  {tag.label}
                </span>
              ))}
            </div>
          )}

          {/* Conteúdo adicional */}
          {children && (
            <div className={styles.content}>
              {children}
            </div>
          )}
        </div>

        {/* Footer com progresso e CTA */}
        <div className={styles.footer}>
          {progress !== undefined && (
            <div className={styles.progress}>
              <div 
                className={styles.progressBar}
                style={{ '--progress': `${progress}%` } as React.CSSProperties}
              />
              <span className={styles.progressLabel}>
                {Math.round(progress)}% completo
              </span>
            </div>
          )}

          {onCtaClick && (
            <button 
              className={styles.cta}
              onClick={onCtaClick}
              aria-label={ctaText}
            >
              <span className={styles.ctaText}>{ctaText}</span>
              <svg className={styles.ctaIcon} viewBox="0 0 16 16">
                <path fill="currentColor" d="M8 0L6.59 1.41 12.17 7H0v2h12.17l-5.58 5.59L8 16l8-8z" />
              </svg>
            </button>
          )}
        </div>
      </CardContainer>
    </article>
  );
}
```

```css
/* src/app/components/ui/AuraCard/AuraCard.module.css */
.card {
  --card-padding: var(--space-4);
  --card-border: var(--border-width) solid var(--border-subtle);
  --card-radius: var(--border-radius);
  
  display: flex;
  flex-direction: column;
  background: var(--surface);
  border: var(--card-border);
  border-radius: var(--card-radius);
  transition: all var(--duration-300) var(--ease-out);
  position: relative;
  overflow: hidden;
}

/* Variantes de densidade */
.density-light {
  --card-padding: var(--space-3);
}

.density-ideal {
  --card-padding: var(--space-4);
}

.density-dense {
  --card-padding: var(--space-3);
  gap: var(--space-2);
}

/* Variantes de tipo */
.project {
  --card-shadow: var(--shadow);
}

.tool {
  --card-border: var(--border-width) solid transparent;
  background: var(--gray-50);
}

.principle {
  --card-border: 2px solid var(--border-subtle);
}

.compact {
  --card-padding: var(--space-3);
  flex-direction: row;
  align-items: flex-start;
  gap: var(--space-3);
}

/* Link container */
.cardLink {
  display: contents;
  text-decoration: none;
  color: inherit;
}

.cardContainer {
  display: flex;
  flex-direction: column;
  height: 100%;
}

/* Hover states */
.card:hover:not(.compact) {
  border-color: var(--blue-technical);
  box-shadow: var(--shadow-md);
  transform: translateY(-2px);
}

.compact:hover {
  background: var(--gray-50);
}

/* Header */
.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--card-padding) var(--card-padding) 0;
  margin-bottom: var(--space-3);
}

.category {
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  color: var(--blue-technical);
  padding: 2px 8px;
  background: rgba(26, 54, 93, 0.1);
  border-radius: var(--border-radius-sm);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.time {
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  color: var(--gray-500);
}

/* Body */
.body {
  padding: 0 var(--card-padding);
  flex: 1;
}

.title {
  font-family: var(--font-sans);
  font-size: var(--text-lg);
  font-weight: 600;
  color: var(--text-primary);
  margin: 0 0 var(--space-2);
  line-height: var(--leading-tight);
}

.excerpt {
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  color: var(--text-secondary);
  line-height: var(--leading-relaxed);
  margin: 0 0 var(--space-3);
}

/* Tags */
.tags {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-2);
  margin-bottom: var(--space-3);
}

.tag {
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  padding: 2px 6px;
  border-radius: var(--border-radius-sm);
  background: var(--gray-100);
  color: var(--gray-700);
}

.tag[data-type="complexity"] {
  background: rgba(197, 48, 48, 0.1);
  color: var(--red-measurement);
}

.tag[data-type="impact"] {
  background: rgba(26, 54, 93, 0.1);
  color: var(--blue-technical);
}

.tag[data-type="status"] {
  background: rgba(72, 187, 120, 0.1);
  color: #48bb78;
}

/* Content area */
.content {
  margin-top: var(--space-3);
  padding-top: var(--space-3);
  border-top: var(--border-width) solid var(--border-subtle);
}

/* Footer */
.footer {
  padding: var(--card-padding);
  border-top: var(--border-width) solid var(--border-subtle);
  margin-top: auto;
}

/* Progress indicator */
.progress {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
  margin-bottom: var(--space-3);
}

.progressBar {
  height: 2px;
  background: var(--gray-200);
  border-radius: 1px;
  overflow: hidden;
  position: relative;
}

.progressBar::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  height: 100%;
  width: var(--progress, 0%);
  background: var(--blue-technical);
  transition: width var(--duration-500) var(--ease-out);
}

.progressLabel {
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  color: var(--gray-500);
  text-align: right;
}

/* CTA */
.cta {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  color: var(--blue-technical);
  background: none;
  border: var(--border-width) solid var(--blue-technical);
  border-radius: var(--border-radius);
  padding: var(--space-2) var(--space-3);
  cursor: pointer;
  transition: all var(--duration-200) var(--ease-out);
  margin-left: auto;
}

.cta:hover {
  background: var(--blue-technical);
  color: white;
  transform: translateX(4px);
}

.ctaIcon {
  width: 12px;
  height: 12px;
  transition: transform var(--duration-200) var(--ease-out);
}

.cta:hover .ctaIcon {
  transform: translateX(2px);
}

/* Compact variant specific */
.compact .header,
.compact .body,
.compact .footer {
  padding: 0;
  border: none;
}

.compact .body {
  flex: 1;
}

.compact .footer {
  margin-top: 0;
}

.compact .cta {
  padding: var(--space-1) var(--space-2);
  font-size: var(--text-xs);
}

/* Dark mode */
@media (prefers-color-scheme: dark) {
  .tool {
    background: var(--gray-800);
  }
  
  .compact:hover {
    background: var(--gray-800);
  }
  
  .tag {
    background: var(--gray-700);
    color: var(--gray-300);
  }
  
  .progressBar {
    background: var(--gray-700);
  }
}

/* Responsive */
@media (max-width: 768px) {
  .card {
    --card-padding: var(--space-3);
  }
  
  .compact {
    flex-direction: column;
    gap: var(--space-2);
  }
}
```

---

## **2. COMPONENTE AURA-BUTTON**

```tsx
// src/app/components/ui/AuraButton/AuraButton.tsx
'use client';

import { ButtonHTMLAttributes, ReactNode } from 'react';
import styles from './AuraButton.module.css';

export interface AuraButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'text' | 'outline';
  size?: 'sm' | 'md' | 'lg';
  density?: 'light' | 'ideal' | 'dense';
  loading?: boolean;
  disabled?: boolean;
  fullWidth?: boolean;
  icon?: ReactNode;
  iconPosition?: 'left' | 'right';
  children: ReactNode;
}

export default function AuraButton({
  variant = 'primary',
  size = 'md',
  density = 'ideal',
  loading = false,
  disabled = false,
  fullWidth = false,
  icon,
  iconPosition = 'right',
  children,
  className,
  ...props
}: AuraButtonProps) {
  const buttonClass = [
    styles.button,
    styles[variant],
    styles[size],
    styles[`density-${density}`],
    loading && styles.loading,
    disabled && styles.disabled,
    fullWidth && styles.fullWidth,
    className
  ].filter(Boolean).join(' ');

  return (
    <button
      className={buttonClass}
      disabled={disabled || loading}
      data-state={loading ? 'loading' : disabled ? 'disabled' : 'default'}
      data-precision="high"
      {...props}
    >
      {loading && (
        <span className={styles.loader} aria-hidden="true">
          <svg className={styles.spinner} viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10" fill="none" strokeWidth="3" />
          </svg>
        </span>
      )}
      
      {icon && iconPosition === 'left' && (
        <span className={`${styles.icon} ${styles.iconLeft}`}>
          {icon}
        </span>
      )}
      
      <span className={styles.label}>
        {children}
      </span>
      
      {icon && iconPosition === 'right' && !loading && (
        <span className={`${styles.icon} ${styles.iconRight}`}>
          {icon}
        </span>
      )}
    </button>
  );
}

// Button Icon Component
export function AuraButtonIcon({ children }: { children: ReactNode }) {
  return <span className={styles.buttonIcon}>{children}</span>;
}
```

```css
/* src/app/components/ui/AuraButton/AuraButton.module.css */
.button {
  --button-padding-y: var(--space-2);
  --button-padding-x: var(--space-4);
  --button-font-size: var(--text-sm);
  --button-icon-size: 16px;
  
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  font-family: var(--font-sans);
  font-size: var(--button-font-size);
  font-weight: 500;
  line-height: var(--leading-tight);
  padding: var(--button-padding-y) var(--button-padding-x);
  border: var(--border-width) solid transparent;
  border-radius: var(--border-radius);
  cursor: pointer;
  transition: all var(--duration-200) var(--ease-out);
  position: relative;
  text-align: center;
  user-select: none;
  vertical-align: middle;
  white-space: nowrap;
}

/* Variants */
.primary {
  background: var(--blue-technical);
  color: white;
  border-color: var(--blue-technical);
}

.primary:hover:not(.disabled):not(.loading) {
  background: #0f2b4d;
  border-color: #0f2b4d;
  transform: translateY(-1px);
  box-shadow: var(--shadow);
}

.secondary {
  background: transparent;
  color: var(--blue-technical);
  border-color: var(--blue-technical);
}

.secondary:hover:not(.disabled):not(.loading) {
  background: rgba(26, 54, 93, 0.1);
  transform: translateY(-1px);
}

.text {
  background: transparent;
  color: var(--text-primary);
  border-color: transparent;
  padding: var(--space-1) var(--space-2);
}

.text:hover:not(.disabled):not(.loading) {
  background: var(--gray-100);
  color: var(--blue-technical);
}

.outline {
  background: transparent;
  color: var(--text-primary);
  border-color: var(--border-subtle);
}

.outline:hover:not(.disabled):not(.loading) {
  border-color: var(--blue-technical);
  color: var(--blue-technical);
}

/* Sizes */
.sm {
  --button-padding-y: var(--space-1);
  --button-padding-x: var(--space-3);
  --button-font-size: var(--text-xs);
  --button-icon-size: 14px;
}

.md {
  --button-padding-y: var(--space-2);
  --button-padding-x: var(--space-4);
  --button-font-size: var(--text-sm);
  --button-icon-size: 16px;
}

.lg {
  --button-padding-y: var(--space-3);
  --button-padding-x: var(--space-6);
  --button-font-size: var(--text-base);
  --button-icon-size: 18px;
}

/* Density */
.density-light {
  letter-spacing: 0.02em;
}

.density-ideal {
  letter-spacing: 0.01em;
}

.density-dense {
  letter-spacing: 0;
}

/* States */
.loading {
  cursor: wait;
  opacity: 0.8;
}

.disabled {
  cursor: not-allowed;
  opacity: 0.5;
}

.primary.disabled {
  background: var(--gray-300);
  border-color: var(--gray-300);
  color: var(--gray-500);
}

/* Focus states */
.button:focus-visible {
  outline: 2px solid var(--red-measurement);
  outline-offset: 2px;
}

.button:active:not(.disabled):not(.loading) {
  transform: translateY(0);
}

/* Full width */
.fullWidth {
  width: 100%;
}

/* Loader */
.loader {
  display: flex;
  align-items: center;
  justify-content: center;
}

.spinner {
  width: var(--button-icon-size);
  height: var(--button-icon-size);
  animation: spin 1s linear infinite;
}

.spinner circle {
  stroke: currentColor;
  stroke-linecap: round;
  stroke-dasharray: 80;
  stroke-dashoffset: 60;
}

/* Icon */
.icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: var(--button-icon-size);
  height: var(--button-icon-size);
}

.iconLeft {
  margin-right: calc(var(--space-1) * -1);
}

.iconRight {
  margin-left: calc(var(--space-1) * -1);
}

/* Label */
.label {
  display: flex;
  align-items: center;
  gap: var(--space-1);
}

/* Loading state label */
.loading .label {
  opacity: 0.7;
}

/* Button Icon Component */
.buttonIcon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 16px;
  height: 16px;
}

/* Dark mode adjustments */
@media (prefers-color-scheme: dark) {
  .text:hover:not(.disabled):not(.loading) {
    background: var(--gray-800);
  }
  
  .secondary:hover:not(.disabled):not(.loading) {
    background: rgba(26, 54, 93, 0.2);
  }
}

/* Responsive */
@media (max-width: 768px) {
  .button {
    --button-padding-x: var(--space-3);
  }
  
  .lg {
    --button-padding-y: var(--space-2);
    --button-padding-x: var(--space-4);
  }
}
```

---

## **3. COMPONENTE AURA-FORM**

```tsx
// src/app/components/ui/AuraForm/AuraForm.tsx
'use client';

import { 
  InputHTMLAttributes, 
  TextareaHTMLAttributes, 
  SelectHTMLAttributes,
  ReactNode,
  useState,
  ChangeEvent
} from 'react';
import AuraButton from '../AuraButton/AuraButton';
import styles from './AuraForm.module.css';

// Common form element props
interface BaseFormProps {
  label?: string;
  hint?: string;
  error?: string;
  success?: string;
  density?: 'light' | 'ideal' | 'dense';
  required?: boolean;
  className?: string;
}

// Input Component
export interface AuraInputProps extends InputHTMLAttributes<HTMLInputElement>, BaseFormProps {
  type?: 'text' | 'email' | 'password' | 'tel' | 'url' | 'number';
  icon?: ReactNode;
}

export function AuraInput({
  label,
  hint,
  error,
  success,
  density = 'ideal',
  required,
  icon,
  className,
  id,
  ...props
}: AuraInputProps) {
  const [isFocused, setIsFocused] = useState(false);
  const [value, setValue] = useState(props.value || props.defaultValue || '');
  
  const inputId = id || `input-${Math.random().toString(36).substr(2, 9)}`;
  const hasError = !!error;
  const hasSuccess = !!success && !hasError;
  
  const handleChange = (e: ChangeEvent<HTMLInputElement>) => {
    setValue(e.target.value);
    props.onChange?.(e);
  };

  return (
    <div 
      className={`${styles.formGroup} ${styles[density]} ${className || ''}`}
      data-state={hasError ? 'error' : hasSuccess ? 'success' : isFocused ? 'focused' : 'default'}
    >
      {label && (
        <label htmlFor={inputId} className={styles.label}>
          <span className={styles.labelText}>
            {label}
            {required && <span className={styles.required}>*</span>}
          </span>
          {hint && <span className={styles.hint}>{hint}</span>}
        </label>
      )}

      <div className={styles.field}>
        {icon && (
          <div className={styles.iconContainer}>
            {icon}
          </div>
        )}
        
        <input
          id={inputId}
          className={`${styles.input} ${icon ? styles.withIcon : ''}`}
          value={value}
          onChange={handleChange}
          onFocus={(e) => {
            setIsFocused(true);
            props.onFocus?.(e);
          }}
          onBlur={(e) => {
            setIsFocused(false);
            props.onBlur?.(e);
          }}
          required={required}
          aria-invalid={hasError}
          aria-describedby={
            [error && `${inputId}-error`, hint && `${inputId}-hint`]
              .filter(Boolean)
              .join(' ')
          }
          {...props}
        />
      </div>

      {/* Feedback */}
      <div className={styles.feedback}>
        {error && (
          <div id={`${inputId}-error`} className={styles.error} role="alert">
            <svg className={styles.feedbackIcon} viewBox="0 0 16 16">
              <path fill="currentColor" d="M8 1a7 7 0 100 14A7 7 0 008 1zm0 12.5a1.5 1.5 0 110-3 1.5 1.5 0 010 3zm1-5.5H7V4h2v4z" />
            </svg>
            {error}
          </div>
        )}
        
        {success && !error && (
          <div id={`${inputId}-success`} className={styles.success}>
            <svg className={styles.feedbackIcon} viewBox="0 0 16 16">
              <path fill="currentColor" d="M8 1a7 7 0 100 14A7 7 0 008 1zm3.7 5.3L7 11 4.3 8.3l1.4-1.4L7 8.2l3.3-3.3 1.4 1.4z" />
            </svg>
            {success}
          </div>
        )}
        
        {hint && !error && !success && (
          <div id={`${inputId}-hint`} className={styles.hintText}>
            {hint}
          </div>
        )}
      </div>
    </div>
  );
}

// Textarea Component
export interface AuraTextareaProps extends TextareaHTMLAttributes<HTMLTextAreaElement>, BaseFormProps {}

export function AuraTextarea({
  label,
  hint,
  error,
  success,
  density = 'ideal',
  required,
  className,
  id,
  ...props
}: AuraTextareaProps) {
  const [isFocused, setIsFocused] = useState(false);
  const [value, setValue] = useState(props.value || '');
  
  const textareaId = id || `textarea-${Math.random().toString(36).substr(2, 9)}`;
  const hasError = !!error;
  const hasSuccess = !!success && !hasError;

  const handleChange = (e: ChangeEvent<HTMLTextAreaElement>) => {
    setValue(e.target.value);
    props.onChange?.(e);
  };

  return (
    <div 
      className={`${styles.formGroup} ${styles[density]} ${className || ''}`}
      data-state={hasError ? 'error' : hasSuccess ? 'success' : isFocused ? 'focused' : 'default'}
    >
      {label && (
        <label htmlFor={textareaId} className={styles.label}>
          <span className={styles.labelText}>
            {label}
            {required && <span className={styles.required}>*</span>}
          </span>
          {hint && <span className={styles.hint}>{hint}</span>}
        </label>
      )}

      <div className={styles.field}>
        <textarea
          id={textareaId}
          className={styles.textarea}
          value={value}
          onChange={handleChange}
          onFocus={(e) => {
            setIsFocused(true);
            props.onFocus?.(e);
          }}
          onBlur={(e) => {
            setIsFocused(false);
            props.onBlur?.(e);
          }}
          required={required}
          aria-invalid={hasError}
          aria-describedby={
            [error && `${textareaId}-error`, hint && `${textareaId}-hint`]
              .filter(Boolean)
              .join(' ')
          }
          {...props}
        />
        
        <div className={styles.textareaCounter}>
          {value.toString().length}/{props.maxLength || '∞'}
        </div>
      </div>

      {/* Feedback */}
      <div className={styles.feedback}>
        {error && (
          <div id={`${textareaId}-error`} className={styles.error} role="alert">
            <svg className={styles.feedbackIcon} viewBox="0 0 16 16">
              <path fill="currentColor" d="M8 1a7 7 0 100 14A7 7 0 008 1zm0 12.5a1.5 1.5 0 110-3 1.5 1.5 0 010 3zm1-5.5H7V4h2v4z" />
            </svg>
            {error}
          </div>
        )}
        
        {success && !error && (
          <div id={`${textareaId}-success`} className={styles.success}>
            <svg className={styles.feedbackIcon} viewBox="0 0 16 16">
              <path fill="currentColor" d="M8 1a7 7 0 100 14A7 7 0 008 1zm3.7 5.3L7 11 4.3 8.3l1.4-1.4L7 8.2l3.3-3.3 1.4 1.4z" />
            </svg>
            {success}
          </div>
        )}
        
        {hint && !error && !success && (
          <div id={`${textareaId}-hint`} className={styles.hintText}>
            {hint}
          </div>
        )}
      </div>
    </div>
  );
}

// Select Component
export interface AuraSelectProps extends SelectHTMLAttributes<HTMLSelectElement>, BaseFormProps {
  options: Array<{
    value: string;
    label: string;
    disabled?: boolean;
  }>;
  icon?: ReactNode;
}

export function AuraSelect({
  label,
  hint,
  error,
  success,
  density = 'ideal',
  required,
  options,
  icon,
  className,
  id,
  ...props
}: AuraSelectProps) {
  const [isFocused, setIsFocused] = useState(false);
  const [value, setValue] = useState(props.value || '');
  
  const selectId = id || `select-${Math.random().toString(36).substr(2, 9)}`;
  const hasError = !!error;
  const hasSuccess = !!success && !hasError;

  const handleChange = (e: ChangeEvent<HTMLSelectElement>) => {
    setValue(e.target.value);
    props.onChange?.(e);
  };

  return (
    <div 
      className={`${styles.formGroup} ${styles[density]} ${className || ''}`}
      data-state={hasError ? 'error' : hasSuccess ? 'success' : isFocused ? 'focused' : 'default'}
    >
      {label && (
        <label htmlFor={selectId} className={styles.label}>
          <span className={styles.labelText}>
            {label}
            {required && <span className={styles.required}>*</span>}
          </span>
          {hint && <span className={styles.hint}>{hint}</span>}
        </label>
      )}

      <div className={styles.field}>
        {icon && (
          <div className={styles.iconContainer}>
            {icon}
          </div>
        )}
        
        <div className={styles.selectWrapper}>
          <select
            id={selectId}
            className={`${styles.select} ${icon ? styles.withIcon : ''}`}
            value={value}
            onChange={handleChange}
            onFocus={(e) => {
              setIsFocused(true);
              props.onFocus?.(e);
            }}
            onBlur={(e) => {
              setIsFocused(false);
              props.onBlur?.(e);
            }}
            required={required}
            aria-invalid={hasError}
            aria-describedby={
              [error && `${selectId}-error`, hint && `${selectId}-hint`]
                .filter(Boolean)
                .join(' ')
            }
            {...props}
          >
            <option value="" disabled={required}>
              {props.placeholder || 'Selecione uma opção'}
            </option>
            {options.map((option) => (
              <option 
                key={option.value} 
                value={option.value}
                disabled={option.disabled}
              >
                {option.label}
              </option>
            ))}
          </select>
          
          <svg className={styles.selectIcon} viewBox="0 0 16 16">
            <path fill="currentColor" d="M8 11L3 6h10z" />
          </svg>
        </div>
      </div>

      {/* Feedback */}
      <div className={styles.feedback}>
        {error && (
          <div id={`${selectId}-error`} className={styles.error} role="alert">
            <svg className={styles.feedbackIcon} viewBox="0 0 16 16">
              <path fill="currentColor" d="M8 1a7 7 0 100 14A7 7 0 008 1zm0 12.5a1.5 1.5 0 110-3 1.5 1.5 0 010 3zm1-5.5H7V4h2v4z" />
            </svg>
            {error}
          </div>
        )}
        
        {success && !error && (
          <div id={`${selectId}-success`} className={styles.success}>
            <svg className={styles.feedbackIcon} viewBox="0 0 16 16">
              <path fill="currentColor" d="M8 1a7 7 0 100 14A7 7 0 008 1zm3.7 5.3L7 11 4.3 8.3l1.4-1.4L7 8.2l3.3-3.3 1.4 1.4z" />
            </svg>
            {success}
          </div>
        )}
        
        {hint && !error && !success && (
          <div id={`${selectId}-hint`} className={styles.hintText}>
            {hint}
          </div>
        )}
      </div>
    </div>
  );
}

// Checkbox Component
export interface AuraCheckboxProps extends InputHTMLAttributes<HTMLInputElement>, BaseFormProps {
  description?: string;
}

export function AuraCheckbox({
  label,
  hint,
  error,
  success,
  description,
  density = 'ideal',
  required,
  className,
  id,
  ...props
}: AuraCheckboxProps) {
  const [isChecked, setIsChecked] = useState(props.checked || false);
  
  const checkboxId = id || `checkbox-${Math.random().toString(36).substr(2, 9)}`;
  const hasError = !!error;
  const hasSuccess = !!success && !hasError;

  const handleChange = (e: ChangeEvent<HTMLInputElement>) => {
    setIsChecked(e.target.checked);
    props.onChange?.(e);
  };

  return (
    <div 
      className={`${styles.formGroup} ${styles.checkboxGroup} ${styles[density]} ${className || ''}`}
      data-state={hasError ? 'error' : hasSuccess ? 'success' : 'default'}
    >
      <div className={styles.checkboxWrapper}>
        <input
          id={checkboxId}
          type="checkbox"
          className={styles.checkbox}
          checked={isChecked}
          onChange={handleChange}
          required={required}
          aria-invalid={hasError}
          aria-describedby={
            [error && `${checkboxId}-error`, hint && `${checkboxId}-hint`, description && `${checkboxId}-description`]
              .filter(Boolean)
              .join(' ')
          }
          {...props}
        />
        
        <label htmlFor={checkboxId} className={styles.checkboxLabel}>
          <div className={styles.checkboxBox}>
            {isChecked && (
              <svg className={styles.checkboxIcon} viewBox="0 0 16 16">
                <path fill="currentColor" d="M13.5 4L6 11.5 2.5 8l1.1-1.1L6 9.3l6.4-6.4 1.1 1.1z" />
              </svg>
            )}
          </div>
          
          <div className={styles.checkboxContent}>
            <span className={styles.checkboxLabelText}>
              {label}
              {required && <span className={styles.required}>*</span>}
            </span>
            
            {description && (
              <div id={`${checkboxId}-description`} className={styles.checkboxDescription}>
                {description}
              </div>
            )}
          </div>
        </label>
      </div>

      {/* Feedback */}
      <div className={styles.feedback}>
        {error && (
          <div id={`${checkboxId}-error`} className={styles.error} role="alert">
            <svg className={styles.feedbackIcon} viewBox="0 0 16 16">
              <path fill="currentColor" d="M8 1a7 7 0 100 14A7 7 0 008 1zm0 12.5a1.5 1.5 0 110-3 1.5 1.5 0 010 3zm1-5.5H7V4h2v4z" />
            </svg>
            {error}
          </div>
        )}
        
        {success && !error && (
          <div id={`${checkboxId}-success`} className={styles.success}>
            <svg className={styles.feedbackIcon} viewBox="0 0 16 16">
              <path fill="currentColor" d="M8 1a7 7 0 100 14A7 7 0 008 1zm3.7 5.3L7 11 4.3 8.3l1.4-1.4L7 8.2l3.3-3.3 1.4 1.4z" />
            </svg>
            {success}
          </div>
        )}
        
        {hint && !error && !success && (
          <div id={`${checkboxId}-hint`} className={styles.hintText}>
            {hint}
          </div>
        )}
      </div>
    </div>
  );
}

// Form Container
interface AuraFormProps {
  children: ReactNode;
  onSubmit?: (data: FormData) => void | Promise<void>;
  className?: string;
}

export function AuraForm({ children, onSubmit, className }: AuraFormProps) {
  const handleSubmit = async (e: React.FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    
    if (onSubmit) {
      const formData = new FormData(e.currentTarget);
      await onSubmit(formData);
    }
  };

  return (
    <form 
      className={`${styles.form} ${className || ''}`}
      onSubmit={handleSubmit}
      data-precision="high"
    >
      {children}
    </form>
  );
}

// Form Actions Component
interface AuraFormActionsProps {
  children: ReactNode;
  align?: 'left' | 'center' | 'right';
}

export function AuraFormActions({ children, align = 'right' }: AuraFormActionsProps) {
  return (
    <div className={`${styles.formActions} ${styles[align]}`}>
      {children}
    </div>
  );
}

// Export all components
export default {
  Form: AuraForm,
  Input: AuraInput,
  Textarea: AuraTextarea,
  Select: AuraSelect,
  Checkbox: AuraCheckbox,
  Actions: AuraFormActions,
  Button: AuraButton,
};
```

```css
/* src/app/components/ui/AuraForm/AuraForm.module.css */
.form {
  width: 100%;
}

.formGroup {
  --form-group-gap: var(--space-2);
  --input-padding: var(--space-3);
  --input-height: 48px;
  
  display: flex;
  flex-direction: column;
  gap: var(--form-group-gap);
  margin-bottom: var(--space-4);
}

/* Density variations */
.light {
  --form-group-gap: var(--space-1);
  --input-padding: var(--space-2);
  --input-height: 40px;
}

.ideal {
  --form-group-gap: var(--space-2);
  --input-padding: var(--space-3);
  --input-height: 48px;
}

.dense {
  --form-group-gap: var(--space-1);
  --input-padding: var(--space-2);
  --input-height: 40px;
  margin-bottom: var(--space-3);
}

/* Label */
.label {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
}

.labelText {
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  font-weight: 500;
  color: var(--text-primary);
  display: flex;
  align-items: center;
  gap: var(--space-1);
}

.required {
  color: var(--red-measurement);
  font-size: var(--text-lg);
  line-height: 1;
}

.hint {
  font-family: var(--font-sans);
  font-size: var(--text-xs);
  color: var(--gray-500);
}

/* Field container */
.field {
  position: relative;
  display: flex;
  align-items: center;
}

/* Icon container */
.iconContainer {
  position: absolute;
  left: var(--input-padding);
  display: flex;
  align-items: center;
  justify-content: center;
  width: 20px;
  height: 20px;
  color: var(--gray-500);
  pointer-events: none;
  z-index: 1;
}

/* Common input styles */
.input,
.textarea,
.select {
  width: 100%;
  font-family: var(--font-sans);
  font-size: var(--text-base);
  color: var(--text-primary);
  background: var(--surface);
  border: var(--border-width) solid var(--border-subtle);
  border-radius: var(--border-radius);
  transition: all var(--duration-200) var(--ease-out);
}

.input:focus,
.textarea:focus,
.select:focus {
  outline: none;
  border-color: var(--blue-technical);
  box-shadow: 0 0 0 3px rgba(26, 54, 93, 0.1);
}

/* Specific input styles */
.input {
  height: var(--input-height);
  padding: 0 var(--input-padding);
  line-height: var(--input-height);
}

.input.withIcon {
  padding-left: calc(var(--input-padding) * 2 + 20px);
}

/* Textarea */
.textarea {
  min-height: 120px;
  padding: var(--input-padding);
  resize: vertical;
  line-height: var(--leading-relaxed);
}

.textareaCounter {
  position: absolute;
  bottom: var(--space-2);
  right: var(--space-2);
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  color: var(--gray-500);
  background: var(--surface);
  padding: 2px 6px;
  border-radius: var(--border-radius-sm);
}

/* Select */
.selectWrapper {
  position: relative;
  width: 100%;
}

.select {
  height: var(--input-height);
  padding: 0 var(--input-padding);
  appearance: none;
  cursor: pointer;
  padding-right: calc(var(--input-padding) * 2 + 20px);
}

.select.withIcon {
  padding-left: calc(var(--input-padding) * 2 + 20px);
}

.selectIcon {
  position: absolute;
  right: var(--input-padding);
  top: 50%;
  transform: translateY(-50%);
  width: 16px;
  height: 16px;
  color: var(--gray-500);
  pointer-events: none;
}

/* Checkbox */
.checkboxGroup {
  margin-bottom: var(--space-3);
}

.checkboxWrapper {
  display: flex;
  align-items: flex-start;
}

.checkbox {
  position: absolute;
  opacity: 0;
  width: 0;
  height: 0;
}

.checkboxLabel {
  display: flex;
  align-items: flex-start;
  gap: var(--space-3);
  cursor: pointer;
  user-select: none;
}

.checkboxBox {
  width: 20px;
  height: 20px;
  flex-shrink: 0;
  border: var(--border-width) solid var(--border-subtle);
  border-radius: var(--border-radius-sm);
  background: var(--surface);
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all var(--duration-200) var(--ease-out);
}

.checkbox:checked + .checkboxLabel .checkboxBox {
  background: var(--blue-technical);
  border-color: var(--blue-technical);
}

.checkbox:focus-visible + .checkboxLabel .checkboxBox {
  outline: 2px solid var(--red-measurement);
  outline-offset: 2px;
}

.checkboxIcon {
  width: 12px;
  height: 12px;
  color: white;
}

.checkboxContent {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
  flex: 1;
}

.checkboxLabelText {
  font-family: var(--font-sans);
  font-size: var(--text-base);
  font-weight: 500;
  color: var(--text-primary);
  line-height: var(--leading-tight);
}

.checkboxDescription {
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  color: var(--text-secondary);
  line-height: var(--leading-relaxed);
}

/* Feedback */
.feedback {
  min-height: 20px;
}

.error {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  color: var(--red-measurement);
  padding: var(--space-1) var(--space-2);
  background: rgba(197, 48, 48, 0.1);
  border-radius: var(--border-radius);
}

.success {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  color: #38a169;
  padding: var(--space-1) var(--space-2);
  background: rgba(56, 161, 105, 0.1);
  border-radius: var(--border-radius);
}

.hintText {
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  color: var(--gray-500);
  padding: var(--space-1) var(--space-2);
}

.feedbackIcon {
  width: 16px;
  height: 16px;
  flex-shrink: 0;
}

/* Form states */
.formGroup[data-state="error"] .input,
.formGroup[data-state="error"] .textarea,
.formGroup[data-state="error"] .select {
  border-color: var(--red-measurement);
}

.formGroup[data-state="error"] .input:focus,
.formGroup[data-state="error"] .textarea:focus,
.formGroup[data-state="error"] .select:focus {
  box-shadow: 0 0 0 3px rgba(197, 48, 48, 0.1);
}

.formGroup[data-state="success"] .input,
.formGroup[data-state="success"] .textarea,
.formGroup[data-state="success"] .select {
  border-color: #38a169;
}

.formGroup[data-state="success"] .input:focus,
.formGroup[data-state="success"] .textarea:focus,
.formGroup[data-state="success"] .select:focus {
  box-shadow: 0 0 0 3px rgba(56, 161, 105, 0.1);
}

/* Form Actions */
.formActions {
  display: flex;
  gap: var(--space-3);
  margin-top: var(--space-6);
  padding-top: var(--space-4);
  border-top: var(--border-width) solid var(--border-subtle);
}

.left {
  justify-content: flex-start;
}

.center {
  justify-content: center;
}

.right {
  justify-content: flex-end;
}

/* Dark mode adjustments */
@media (prefers-color-scheme: dark) {
  .input,
  .textarea,
  .select {
    background: var(--gray-800);
    border-color: var(--gray-700);
  }
  
  .checkboxBox {
    background: var(--gray-800);
    border-color: var(--gray-700);
  }
  
  .input:focus,
  .textarea:focus,
  .select:focus {
    box-shadow: 0 0 0 3px rgba(26, 54, 93, 0.3);
  }
  
  .textareaCounter {
    background: var(--gray-800);
  }
}

/* Responsive */
@media (max-width: 768px) {
  .formGroup {
    --input-height: 44px;
  }
  
  .formActions {
    flex-direction: column;
  }
  
  .formActions.right,
  .formActions.left,
  .formActions.center {
    justify-content: stretch;
  }
}
```

---

## **4. COMPONENTE AURA-MODAL**

```tsx
// src/app/components/ui/AuraModal/AuraModal.tsx
'use client';

import { ReactNode, useEffect, useRef } from 'react';
import { createPortal } from 'react-dom';
import AuraButton from '../AuraButton/AuraButton';
import styles from './AuraModal.module.css';

export interface AuraModalProps {
  isOpen: boolean;
  onClose: () => void;
  title?: string;
  children: ReactNode;
  size?: 'sm' | 'md' | 'lg' | 'full';
  showCloseButton?: boolean;
  closeOnOverlayClick?: boolean;
  closeOnEsc?: boolean;
  actions?: ReactNode;
  className?: string;
}

export default function AuraModal({
  isOpen,
  onClose,
  title,
  children,
  size = 'md',
  showCloseButton = true,
  closeOnOverlayClick = true,
  closeOnEsc = true,
  actions,
  className,
}: AuraModalProps) {
  const modalRef = useRef<HTMLDivElement>(null);
  const previousFocusRef = useRef<HTMLElement | null>(null);

  useEffect(() => {
    if (!isOpen) return;

    // Store previous focus
    previousFocusRef.current = document.activeElement as HTMLElement;

    // Focus trap
    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.key === 'Escape' && closeOnEsc) {
        onClose();
      }

      if (e.key === 'Tab' && modalRef.current) {
        const focusableElements = modalRef.current.querySelectorAll(
          'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
        );

        const firstElement = focusableElements[0] as HTMLElement;
        const lastElement = focusableElements[focusableElements.length - 1] as HTMLElement;

        if (e.shiftKey && document.activeElement === firstElement) {
          e.preventDefault();
          lastElement.focus();
        } else if (!e.shiftKey && document.activeElement === lastElement) {
          e.preventDefault();
          firstElement.focus();
        }
      }
    };

    document.addEventListener('keydown', handleKeyDown);
    document.body.style.overflow = 'hidden';

    // Focus first element
    setTimeout(() => {
      const firstFocusable = modalRef.current?.querySelector(
        'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
      ) as HTMLElement;
      firstFocusable?.focus();
    }, 100);

    return () => {
      document.removeEventListener('keydown', handleKeyDown);
      document.body.style.overflow = '';

      // Restore previous focus
      previousFocusRef.current?.focus();
    };
  }, [isOpen, onClose, closeOnEsc]);

  if (!isOpen) return null;

  const handleOverlayClick = (e: React.MouseEvent) => {
    if (closeOnOverlayClick && e.target === e.currentTarget) {
      onClose();
    }
  };

  const modalContent = (
    <div 
      className={`${styles.modalOverlay} ${isOpen ? styles.open : ''}`}
      onClick={handleOverlayClick}
      role="dialog"
      aria-modal="true"
      aria-labelledby={title ? 'modal-title' : undefined}
    >
      <div 
        ref={modalRef}
        className={`${styles.modal} ${styles[size]} ${className || ''}`}
        data-precision="high"
      >
        {/* Header */}
        {(title || showCloseButton) && (
          <div className={styles.header}>
            {title && (
              <h2 id="modal-title" className={styles.title}>
                {title}
              </h2>
            )}
            
            {showCloseButton && (
              <AuraButton
                variant="text"
                size="sm"
                onClick={onClose}
                className={styles.closeButton}
                aria-label="Fechar modal"
              >
                <svg className={styles.closeIcon} viewBox="0 0 24 24">
                  <path fill="currentColor" d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z" />
                </svg>
              </AuraButton>
            )}
          </div>
        )}

        {/* Content */}
        <div className={styles.content}>
          {children}
        </div>

        {/* Footer with actions */}
        {actions && (
          <div className={styles.footer}>
            {actions}
          </div>
        )}
      </div>
    </div>
  );

  return createPortal(modalContent, document.body);
}

// Hook para usar modal facilmente
export function useAuraModal() {
  const [isOpen, setIsOpen] = useState(false);

  const open = () => setIsOpen(true);
  const close = () => setIsOpen(false);
  const toggle = () => setIsOpen(!isOpen);

  return {
    isOpen,
    open,
    close,
    toggle,
    Modal: (props: Omit<AuraModalProps, 'isOpen' | 'onClose'>) => (
      <AuraModal isOpen={isOpen} onClose={close} {...props} />
    ),
  };
}
```

```css
/* src/app/components/ui/AuraModal/AuraModal.module.css */
.modalOverlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--space-4);
  background: rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(4px);
  opacity: 0;
  animation: modal-fade-in 0.3s var(--ease-out) forwards;
}

@keyframes modal-fade-in {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

.modal {
  --modal-padding: var(--space-6);
  
  background: var(--surface);
  border-radius: var(--border-radius-lg);
  box-shadow: var(--shadow-lg);
  max-height: 90vh;
  display: flex;
  flex-direction: column;
  animation: modal-slide-up 0.4s var(--ease-out) forwards;
  transform: translateY(20px);
}

@keyframes modal-slide-up {
  from {
    transform: translateY(20px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

/* Sizes */
.sm {
  width: 100%;
  max-width: 400px;
}

.md {
  width: 100%;
  max-width: 600px;
}

.lg {
  width: 100%;
  max-width: 800px;
}

.full {
  width: 100%;
  max-width: 1200px;
}

/* Header */
.header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: var(--modal-padding) var(--modal-padding) 0;
  margin-bottom: var(--space-4);
}

.title {
  font-family: var(--font-serif);
  font-size: var(--text-2xl);
  font-weight: 400;
  color: var(--text-primary);
  margin: 0;
  line-height: var(--leading-tight);
  flex: 1;
}

.closeButton {
  margin-top: -8px;
  margin-right: -8px;
}

.closeIcon {
  width: 20px;
  height: 20px;
}

/* Content */
.content {
  padding: 0 var(--modal-padding);
  flex: 1;
  overflow-y: auto;
  max-height: calc(90vh - 200px);
}

/* Custom scrollbar for modal */
.content::-webkit-scrollbar {
  width: 6px;
}

.content::-webkit-scrollbar-track {
  background: transparent;
}

.content::-webkit-scrollbar-thumb {
  background: var(--gray-300);
  border-radius: 3px;
}

.content::-webkit-scrollbar-thumb:hover {
  background: var(--gray-400);
}

/* Footer */
.footer {
  padding: var(--space-6) var(--modal-padding) var(--modal-padding);
  border-top: var(--border-width) solid var(--border-subtle);
  margin-top: var(--space-6);
  display: flex;
  justify-content: flex-end;
  gap: var(--space-3);
}

/* Dark mode */
@media (prefers-color-scheme: dark) {
  .modalOverlay {
    background: rgba(0, 0, 0, 0.7);
  }
  
  .content::-webkit-scrollbar-thumb {
    background: var(--gray-600);
  }
  
  .content::-webkit-scrollbar-thumb:hover {
    background: var(--gray-500);
  }
}

/* Responsive */
@media (max-width: 768px) {
  .modalOverlay {
    padding: var(--space-2);
  }
  
  .modal {
    --modal-padding: var(--space-4);
    max-height: 95vh;
  }
  
  .content {
    max-height: calc(95vh - 180px);
  }
  
  .sm,
  .md,
  .lg,
  .full {
    width: 100%;
    max-width: none;
  }
  
  .footer {
    flex-direction: column;
  }
}
```

---

## **5. COMPONENTE AURA-LOADER**

```tsx
// src/app/components/ui/AuraLoader/AuraLoader.tsx
import styles from './AuraLoader.module.css';

export interface AuraLoaderProps {
  type?: 'spinner' | 'dots' | 'bars' | 'skeleton';
  size?: 'sm' | 'md' | 'lg';
  text?: string;
  fullScreen?: boolean;
  className?: string;
}

export default function AuraLoader({
  type = 'spinner',
  size = 'md',
  text,
  fullScreen = false,
  className,
}: AuraLoaderProps) {
  const loaderClass = [
    styles.loader,
    styles[type],
    styles[size],
    fullScreen && styles.fullScreen,
    className
  ].filter(Boolean).join(' ');

  const content = (
    <div className={loaderClass} data-precision="high" role="status" aria-live="polite">
      {type === 'spinner' && (
        <div className={styles.spinner}>
          <svg className={styles.spinnerCircle} viewBox="0 0 50 50">
            <circle cx="25" cy="25" r="20" fill="none" strokeWidth="4" />
          </svg>
        </div>
      )}
      
      {type === 'dots' && (
        <div className={styles.dots}>
          <div className={styles.dot} />
          <div className={styles.dot} />
          <div className={styles.dot} />
        </div>
      )}
      
      {type === 'bars' && (
        <div className={styles.bars}>
          <div className={styles.bar} />
          <div className={styles.bar} />
          <div className={styles.bar} />
          <div className={styles.bar} />
        </div>
      )}
      
      {type === 'skeleton' && (
        <div className={styles.skeleton}>
          <div className={styles.skeletonHeader} />
          <div className={styles.skeletonBody}>
            <div className={styles.skeletonLine} />
            <div className={styles.skeletonLine} />
            <div className={styles.skeletonLine} />
          </div>
          <div className={styles.skeletonFooter} />
        </div>
      )}
      
      {text && (
        <div className={styles.text}>
          {text}
        </div>
      )}
    </div>
  );

  if (fullScreen) {
    return (
      <div className={styles.fullScreenContainer}>
        {content}
      </div>
    );
  }

  return content;
}
```

```css
/* src/app/components/ui/AuraLoader/AuraLoader.module.css */
.loader {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: var(--space-4);
}

/* Sizes */
.sm {
  gap: var(--space-2);
}

.md {
  gap: var(--space-4);
}

.lg {
  gap: var(--space-6);
}

/* Spinner */
.spinner {
  display: flex;
  align-items: center;
  justify-content: center;
}

.spinnerCircle {
  width: 40px;
  height: 40px;
  animation: spin 1s linear infinite;
}

.spinnerCircle circle {
  stroke: var(--blue-technical);
  stroke-linecap: round;
  stroke-dasharray: 150;
  stroke-dashoffset: -10;
}

.sm .spinnerCircle {
  width: 24px;
  height: 24px;
}

.lg .spinnerCircle {
  width: 60px;
  height: 60px;
}

/* Dots */
.dots {
  display: flex;
  gap: var(--space-2);
  align-items: center;
  justify-content: center;
}

.dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--blue-technical);
  animation: dots-pulse 1.4s ease-in-out infinite both;
}

.dot:nth-child(2) {
  animation-delay: 0.2s;
}

.dot:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes dots-pulse {
  0%, 80%, 100% {
    opacity: 0.3;
    transform: scale(0.8);
  }
  40% {
    opacity: 1;
    transform: scale(1);
  }
}

.sm .dot {
  width: 6px;
  height: 6px;
}

.lg .dot {
  width: 12px;
  height: 12px;
}

/* Bars */
.bars {
  display: flex;
  gap: 3px;
  align-items: flex-end;
  height: 32px;
}

.bar {
  width: 6px;
  height: 100%;
  background: var(--blue-technical);
  animation: bars-wave 1.2s ease-in-out infinite both;
  border-radius: 1px;
}

.bar:nth-child(1) { animation-delay: -1.2s; }
.bar:nth-child(2) { animation-delay: -1.1s; }
.bar:nth-child(3) { animation-delay: -1.0s; }
.bar:nth-child(4) { animation-delay: -0.9s; }

@keyframes bars-wave {
  0%, 40%, 100% {
    transform: scaleY(0.4);
  }
  20% {
    transform: scaleY(1);
  }
}

.sm .bars {
  height: 24px;
}

.sm .bar {
  width: 4px;
}

.lg .bars {
  height: 48px;
}

.lg .bar {
  width: 8px;
}

/* Skeleton */
.skeleton {
  width: 100%;
  max-width: 400px;
  padding: var(--space-4);
  background: var(--surface);
  border: var(--border-width) solid var(--border-subtle);
  border-radius: var(--border-radius);
}

.skeletonHeader {
  width: 40%;
  height: 20px;
  background: var(--gray-200);
  border-radius: var(--border-radius-sm);
  margin-bottom: var(--space-4);
  animation: skeleton-pulse 1.5s ease-in-out infinite;
}

.skeletonBody {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
}

.skeletonLine {
  height: 16px;
  background: var(--gray-200);
  border-radius: var(--border-radius-sm);
  animation: skeleton-pulse 1.5s ease-in-out infinite;
}

.skeletonLine:nth-child(1) {
  width: 100%;
}

.skeletonLine:nth-child(2) {
  width: 80%;
}

.skeletonLine:nth-child(3) {
  width: 60%;
}

.skeletonFooter {
  width: 30%;
  height: 20px;
  background: var(--gray-200);
  border-radius: var(--border-radius-sm);
  margin-top: var(--space-4);
  animation: skeleton-pulse 1.5s ease-in-out infinite;
}

@keyframes skeleton-pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.5;
  }
}

/* Text */
.text {
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  color: var(--text-secondary);
  text-align: center;
  max-width: 200px;
}

.sm .text {
  font-size: var(--text-xs);
}

.lg .text {
  font-size: var(--text-base);
}

/* Full screen */
.fullScreenContainer {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 9998;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(247, 250, 252, 0.9);
  backdrop-filter: blur(8px);
}

.fullScreen {
  padding: var(--space-8);
  background: var(--surface);
  border-radius: var(--border-radius-lg);
  border: var(--border-width) solid var(--border-subtle);
  box-shadow: var(--shadow-lg);
}

/* Dark mode */
@media (prefers-color-scheme: dark) {
  .spinnerCircle circle {
    stroke: var(--blue-technical);
  }
  
  .dot {
    background: var(--blue-technical);
  }
  
  .bar {
    background: var(--blue-technical);
  }
  
  .skeletonHeader,
  .skeletonLine,
  .skeletonFooter {
    background: var(--gray-700);
  }
  
  .fullScreenContainer {
    background: rgba(26, 32, 44, 0.9);
  }
}

/* Responsive */
@media (max-width: 768px) {
  .fullScreen {
    padding: var(--space-4);
  }
}
```

---

## **6. COMPONENTE AURA-TOOLTIP**

```tsx
// src/app/components/ui/AuraTooltip/AuraTooltip.tsx
'use client';

import { ReactNode, useState, useRef, useEffect } from 'react';
import styles from './AuraTooltip.module.css';

export interface AuraTooltipProps {
  content: ReactNode;
  children: ReactNode;
  position?: 'top' | 'right' | 'bottom' | 'left';
  delay?: number;
  maxWidth?: number;
  className?: string;
}

export default function AuraTooltip({
  content,
  children,
  position = 'top',
  delay = 300,
  maxWidth = 300,
  className,
}: AuraTooltipProps) {
  const [isVisible, setIsVisible] = useState(false);
  const [coords, setCoords] = useState({ x: 0, y: 0 });
  const tooltipRef = useRef<HTMLDivElement>(null);
  const triggerRef = useRef<HTMLDivElement>(null);
  const timeoutRef = useRef<NodeJS.Timeout>();

  const showTooltip = () => {
    if (timeoutRef.current) {
      clearTimeout(timeoutRef.current);
    }
    timeoutRef.current = setTimeout(() => {
      setIsVisible(true);
      updatePosition();
    }, delay);
  };

  const hideTooltip = () => {
    if (timeoutRef.current) {
      clearTimeout(timeoutRef.current);
    }
    timeoutRef.current = setTimeout(() => {
      setIsVisible(false);
    }, 150);
  };

  const updatePosition = () => {
    if (!triggerRef.current || !tooltipRef.current) return;

    const triggerRect = triggerRef.current.getBoundingClientRect();
    const tooltipRect = tooltipRef.current.getBoundingClientRect();

    let x = 0;
    let y = 0;

    switch (position) {
      case 'top':
        x = triggerRect.left + triggerRect.width / 2 - tooltipRect.width / 2;
        y = triggerRect.top - tooltipRect.height - 8;
        break;
      case 'right':
        x = triggerRect.right + 8;
        y = triggerRect.top + triggerRect.height / 2 - tooltipRect.height / 2;
        break;
      case 'bottom':
        x = triggerRect.left + triggerRect.width / 2 - tooltipRect.width / 2;
        y = triggerRect.bottom + 8;
        break;
      case 'left':
        x = triggerRect.left - tooltipRect.width - 8;
        y = triggerRect.top + triggerRect.height / 2 - tooltipRect.height / 2;
        break;
    }

    // Adjust for window boundaries
    const viewportWidth = window.innerWidth;
    const viewportHeight = window.innerHeight;

    if (x < 8) x = 8;
    if (x + tooltipRect.width > viewportWidth - 8) {
      x = viewportWidth - tooltipRect.width - 8;
    }
    if (y < 8) y = 8;
    if (y + tooltipRect.height > viewportHeight - 8) {
      y = viewportHeight - tooltipRect.height - 8;
    }

    setCoords({ x, y });
  };

  useEffect(() => {
    if (isVisible) {
      updatePosition();
      window.addEventListener('scroll', updatePosition);
      window.addEventListener('resize', updatePosition);
    }

    return () => {
      window.removeEventListener('scroll', updatePosition);
      window.removeEventListener('resize', updatePosition);
    };
  }, [isVisible, position]);

  useEffect(() => {
    return () => {
      if (timeoutRef.current) {
        clearTimeout(timeoutRef.current);
      }
    };
  }, []);

  return (
    <div className={`${styles.container} ${className || ''}`}>
      <div
        ref={triggerRef}
        className={styles.trigger}
        onMouseEnter={showTooltip}
        onMouseLeave={hideTooltip}
        onFocus={showTooltip}
        onBlur={hideTooltip}
        aria-describedby={isVisible ? 'aura-tooltip' : undefined}
      >
        {children}
      </div>

      {isVisible && (
        <div
          ref={tooltipRef}
          id="aura-tooltip"
          className={`${styles.tooltip} ${styles[position]}`}
          role="tooltip"
          style={{
            left: `${coords.x}px`,
            top: `${coords.y}px`,
            maxWidth: `${maxWidth}px`,
          }}
        >
          <div className={styles.content}>
            {content}
          </div>
          <div className={`${styles.arrow} ${styles[`arrow-${position}`]}`} />
        </div>
      )}
    </div>
  );
}
```

```css
/* src/app/components/ui/AuraTooltip/AuraTooltip.module.css */
.container {
  display: inline-block;
  position: relative;
}

.trigger {
  display: inline-block;
  cursor: help;
}

.tooltip {
  position: fixed;
  z-index: 9999;
  background: var(--gray-900);
  color: white;
  padding: var(--space-3) var(--space-4);
  border-radius: var(--border-radius);
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  line-height: var(--leading-relaxed);
  box-shadow: var(--shadow-lg);
  pointer-events: none;
  animation: tooltip-fade-in 0.2s var(--ease-out);
}

@keyframes tooltip-fade-in {
  from {
    opacity: 0;
    transform: translateY(4px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.content {
  position: relative;
  z-index: 2;
}

/* Arrow */
.arrow {
  position: absolute;
  width: 8px;
  height: 8px;
  background: var(--gray-900);
  transform: rotate(45deg);
}

.arrow-top {
  bottom: -4px;
  left: 50%;
  transform: translateX(-50%) rotate(45deg);
}

.arrow-right {
  left: -4px;
  top: 50%;
  transform: translateY(-50%) rotate(45deg);
}

.arrow-bottom {
  top: -4px;
  left: 50%;
  transform: translateX(-50%) rotate(45deg);
}

.arrow-left {
  right: -4px;
  top: 50%;
  transform: translateY(-50%) rotate(45deg);
}

/* Dark mode - tooltip already dark */
@media (prefers-color-scheme: dark) {
  .tooltip {
    background: var(--gray-800);
    color: var(--gray-100);
  }
  
  .arrow {
    background: var(--gray-800);
  }
}

/* Responsive */
@media (max-width: 768px) {
  .tooltip {
    max-width: 250px !important;
    padding: var(--space-2) var(--space-3);
  }
}
```

---

## **7. EXEMPLO DE USO INTEGRADO**

```tsx
// src/app/exemplos/page.tsx
'use client';

import { useState } from 'react';
import AuraCard from '../components/ui/AuraCard/AuraCard';
import AuraButton from '../components/ui/AuraButton/AuraButton';
import AuraForm from '../components/ui/AuraForm/AuraForm';
import AuraModal from '../components/ui/AuraModal/AuraModal';
import AuraLoader from '../components/ui/AuraLoader/AuraLoader';
import AuraTooltip from '../components/ui/AuraTooltip/AuraTooltip';

export default function ExemplosPage() {
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [isLoading, setIsLoading] = useState(false);
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    message: '',
    subscribe: false,
  });

  const handleSubmit = async (formData: FormData) => {
    setIsLoading(true);
    
    // Simular API call
    await new Promise(resolve => setTimeout(resolve, 2000));
    
    console.log('Form data:', Object.fromEntries(formData));
    setIsLoading(false);
    setIsModalOpen(false);
    
    // Reset form
    setFormData({
      name: '',
      email: '',
      message: '',
      subscribe: false,
    });
  };

  return (
    <div className="container" style={{ padding: 'var(--space-8) var(--space-4)' }}>
      <h1>Exemplos de Componentes Ψ</h1>
      
      {/* Cards */}
      <section style={{ marginBottom: 'var(--space-12)' }}>
        <h2>Sistema de Cards</h2>
        <div style={{ 
          display: 'grid', 
          gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))', 
          gap: 'var(--space-6)',
          marginTop: 'var(--space-6)' 
        }}>
          <AuraCard
            variant="project"
            category="Design System"
            time="Q1 2024"
            title="Ψ-Token System"
            excerpt="Sistema de tokens com densidade calibrada e precisão técnica"
            tags={[
              { label: 'Alta complexidade', type: 'complexity' },
              { label: 'Impacto alto', type: 'impact' },
            ]}
            progress={80}
            ctaText="Analisar método"
            onCtaClick={() => setIsModalOpen(true)}
          />
          
          <AuraCard
            variant="tool"
            density="light"
            title="Figma Ψ-Plugin"
            excerpt="Extensão para análise de densidade visual em tempo real"
            tags={[
              { label: 'Em desenvolvimento', type: 'status' },
            ]}
          />
        </div>
      </section>

      {/* Buttons */}
      <section style={{ marginBottom: 'var(--space-12)' }}>
        <h2>Sistema de Botões</h2>
        <div style={{ 
          display: 'flex', 
          flexWrap: 'wrap', 
          gap: 'var(--space-4)',
          marginTop: 'var(--space-6)' 
        }}>
          <AuraButton variant="primary">
            Primário
          </AuraButton>
          
          <AuraButton variant="secondary">
            Secundário
          </AuraButton>
          
          <AuraButton variant="outline">
            Outline
          </AuraButton>
          
          <AuraButton variant="text">
            Texto
          </AuraButton>
          
          <AuraButton 
            variant="primary" 
            loading={isLoading}
            onClick={() => setIsLoading(!isLoading)}
          >
            {isLoading ? 'Carregando...' : 'Clique para carregar'}
          </AuraButton>
          
          <AuraButton variant="secondary" disabled>
            Desabilitado
          </AuraButton>
        </div>
      </section>

      {/* Form */}
      <section style={{ marginBottom: 'var(--space-12)' }}>
        <h2>Formulário Ψ</h2>
        <div style={{ maxWidth: '500px', marginTop: 'var(--space-6)' }}>
          <AuraForm.Form onSubmit={handleSubmit}>
            <AuraForm.Input
              label="Nome completo"
              name="name"
              placeholder="Seu nome"
              required
              value={formData.name}
              onChange={(e) => setFormData({...formData, name: e.target.value})}
            />
            
            <AuraForm.Input
              label="Email"
              name="email"
              type="email"
              placeholder="seu@email.com"
              required
              hint="Não compartilhamos seu email"
              value={formData.email}
              onChange={(e) => setFormData({...formData, email: e.target.value})}
            />
            
            <AuraForm.Textarea
              label="Mensagem"
              name="message"
              placeholder="Descreva sua necessidade..."
              rows={4}
              value={formData.message}
              onChange={(e) => setFormData({...formData, message: e.target.value})}
            />
            
            <AuraForm.Checkbox
              label="Receber atualizações Ψ"
              name="subscribe"
              checked={formData.subscribe}
              onChange={(e) => setFormData({...formData, subscribe: e.target.checked})}
              description="Notificações sobre novos métodos e ferramentas"
            />
            
            <AuraForm.Actions>
              <AuraButton 
                type="button" 
                variant="text"
                onClick={() => setIsModalOpen(false)}
              >
                Cancelar
              </AuraButton>
              
              <AuraButton 
                type="submit" 
                variant="primary"
                loading={isLoading}
              >
                Enviar mensagem
              </AuraButton>
            </AuraForm.Actions>
          </AuraForm.Form>
        </div>
      </section>

      {/* Tooltip */}
      <section style={{ marginBottom: 'var(--space-12)' }}>
        <h2>Tooltip Ψ</h2>
        <div style={{ marginTop: 'var(--space-6)' }}>
          <AuraTooltip
            content="Precisão técnica com densidade calibrada para máxima clareza"
            position="top"
          >
            <span style={{ 
              borderBottom: '1px dotted var(--blue-technical)',
              color: 'var(--blue-technical)',
              cursor: 'help'
            }}>
              Passe o mouse para ver o tooltip
            </span>
          </AuraTooltip>
        </div>
      </section>

      {/* Loader */}
      <section style={{ marginBottom: 'var(--space-12)' }}>
        <h2>Loaders Ψ</h2>
        <div style={{ 
          display: 'flex', 
          flexWrap: 'wrap', 
          gap: 'var(--space-8)',
          marginTop: 'var(--space-6)' 
        }}>
          <div style={{ textAlign: 'center' }}>
            <AuraLoader type="spinner" size="md" />
            <div style={{ marginTop: 'var(--space-2)', fontSize: 'var(--text-xs)' }}>Spinner</div>
          </div>
          
          <div style={{ textAlign: 'center' }}>
            <AuraLoader type="dots" size="md" />
            <div style={{ marginTop: 'var(--space-2)', fontSize: 'var(--text-xs)' }}>Dots</div>
          </div>
          
          <div style={{ textAlign: 'center' }}>
            <AuraLoader type="bars" size="md" />
            <div style={{ marginTop: 'var(--space-2)', fontSize: 'var(--text-xs)' }}>Bars</div>
          </div>
          
          <div style={{ width: '200px' }}>
            <AuraLoader type="skeleton" />
            <div style={{ marginTop: 'var(--space-2)', fontSize: 'var(--text-xs)' }}>Skeleton</div>
          </div>
        </div>
      </section>

      {/* Modal */}
      <AuraModal
        isOpen={isModalOpen}
        onClose={() => setIsModalOpen(false)}
        title="Detalhes do Projeto"
        size="md"
      >
        <p>Este é um exemplo de modal Ψ com foco na precisão técnica e clareza informacional.</p>
        
        <div style={{ marginTop: 'var(--space-6)' }}>
          <AuraCard
            variant="compact"
            title="Modal Ψ-System"
            excerpt="Sistema de modais com foco em acessibilidade e precisão técnica"
            tags={[
              { label: 'Concluído', type: 'status' },
            ]}
          />
        </div>
      </AuraModal>

      {/* Loader fullscreen (exemplo) */}
      {false && ( // Desabilitado por padrão
        <AuraLoader 
          type="spinner" 
          size="lg" 
          text="Processando solicitação Ψ..." 
          fullScreen 
        />
      )}
    </div>
  );
}
```

---

## **8. ÍNDICE DE COMPONENTES (barrel exports)**

```tsx
// src/app/components/ui/index.ts
// Componentes Ψ-Repeatitivos
export { default as AuraHeader } from './AuraHeader/AuraHeader';
export { default as AuraFooter } from './AuraFooter/AuraFooter';
export { default as AuraCard } from './AuraCard/AuraCard';
export { default as AuraButton } from './AuraButton/AuraButton';
export { default as AuraForm } from './AuraForm/AuraForm';
export { default as AuraModal } from './AuraModal/AuraModal';
export { default as AuraLoader } from './AuraLoader/AuraLoader';
export { default as AuraTooltip } from './AuraTooltip/AuraTooltip';

// Componentes Ψ-Únicos (sections)
export { default as PortalHero } from '../sections/PortalHero/PortalHero';
export { default as PortalProcessPreview } from '../sections/PortalProcessPreview/PortalProcessPreview';

// Utils
export * from './AuraForm/AuraForm';
export { useAuraModal } from './AuraModal/AuraModal';
```

---

## **9. Ψ-VERIFICAÇÃO DA IMPLEMENTAÇÃO**

### **Testes Automatizados (exemplo)**

```typescript
// src/lib/__tests__/aura-components.test.ts
import { render, screen, fireEvent } from '@testing-library/react';
import { AuraButton, AuraCard, AuraForm } from '@/app/components/ui';

describe('Componentes Ψ - Testes de Coerência', () => {
  test('AuraButton mantém estados Ψ corretamente', () => {
    const { rerender } = render(
      <AuraButton variant="primary">Teste</AuraButton>
    );
    
    // Estado E⁺ (normal)
    const button = screen.getByRole('button');
    expect(button).toHaveAttribute('data-state', 'default');
    
    // Estado loading
    rerender(<AuraButton variant="primary" loading>Teste</AuraButton>);
    expect(button).toHaveAttribute('data-state', 'loading');
    
    // Estado disabled
    rerender(<AuraButton variant="primary" disabled>Teste</AuraButton>);
    expect(button).toHaveAttribute('data-state', 'disabled');
  });
  
  test('AuraCard mantém densidade especificada', () => {
    render(
      <AuraCard
        variant="project"
        density="ideal"
        title="Teste"
        excerpt="Descrição"
      />
    );
    
    const card = screen.getByRole('article');
    expect(card).toHaveAttribute('data-density', 'ideal');
  });
  
  test('AuraForm valida entrada corretamente', async () => {
    const handleSubmit = jest.fn();
    
    render(
      <AuraForm.Form onSubmit={handleSubmit}>
        <AuraForm.Input
          label="Email"
          name="email"
          type="email"
          required
        />
      </AuraForm.Form>
    );
    
    const input = screen.getByLabelText(/email/i);
    fireEvent.change(input, { target: { value: 'teste@email.com' } });
    
    // Verificar que não há erro
    expect(screen.queryByRole('alert')).toBeNull();
  });
});
```

---

## **10. ATUALIZAÇÃO DO PACKAGE.JSON**

```json
{
  "dependencies": {
    "next": "14.0.0",
    "react": "^18",
    "react-dom": "^18",
    "@next/font": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "typescript": "^5",
    "eslint": "^8",
    "eslint-config-next": "14.0.0",
    "@testing-library/react": "^14.0.0",
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/user-event": "^14.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "jest",
    "test:watch": "jest --watch"
  }
}
```

---

## **Ψ-DIAGNÓSTICO FINAL DA IMPLEMENTAÇÃO**

✅ **Todos os componentes Ψ-repetitivos implementados**:

1. **AuraHeader** ✅
   - Navegação com precisão
   - Progress indicator
   - Responsivo

2. **AuraFooter** ✅
   - Densidade decrescente
   - Informação essencial
   - Contato discreto

3. **AuraCard** ✅
   - 4 variantes (project, tool, principle, compact)
   - Sistema de tags
   - Progress indicator
   - Densidade calibrada

4. **AuraButton** ✅
   - 4 variantes (primary, secondary, text, outline)
   - 3 tamanhos
   - Estados loading/disabled
   - Densidade ajustável

5. **AuraForm** ✅
   - Input, Textarea, Select, Checkbox
   - Validação em tempo real
   - Feedback Ψ (E⁺, E⁻, Ø)
   - Acessibilidade completa

6. **AuraModal** ✅
   - Focus trap
   - Animações suaves
   - 4 tamanhos
   - Responsivo

7. **AuraLoader** ✅
   - 4 tipos (spinner, dots, bars, skeleton)
   - Full screen option
   - Densidade visual calibrada

8. **AuraTooltip** ✅
   - 4 posições
   - Delay configurável
   - Ajuste automático de posição

---

## **PRÓXIMOS PASSOS Ψ**

Com todos os componentes repetitivos implementados, o próximo passo é:

### **Implementar as 6 páginas restantes**:

1. **`/processo`** - Timeline interativa com expansão de etapas
2. **`/projetos`** - Sistema de filtro + before/after sliders
3. **`/ferramentas`** - Grid categorizado com tooltips
4. **`/principios`** - Sistema comparativo com exemplos
5. **`/sobre`** - Timeline de experiência + referências
6. **`/contato`** - Formulário + calendário integrado

### **Depois, implementar o Observador Canônico Ψ**:
- Sistema de métricas em tempo real
- Ajuste automático de densidade
- Otimização contínua da aura

---

**Comando para próximo passo**: `"implemente a página /processo com a timeline interativa conforme especificado no blueprint Ψ"`